post-receive:
- pattern: glide.*
  run:
  - glide install
prepare-commit-msg:
- pattern: '*.go'
  run:
  - golint -min_confidence 0.3 {file}
  - gocyclo -over 10 {file}
  - varcheck
  - deadcode
  - structcheck
pre-push:
- run:
  - go test .
