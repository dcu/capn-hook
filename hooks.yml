pre-commit:
- pattern: '*.go'
  run:
  - golint -min_confidence 0.3 {file}
  - gocyclo -over 10 {file}
  - varcheck
  - deadcode
  - structcheck
  enforce: true
post-receive:
- pattern: glide.*
  run:
  - glide install
pre-push:
- run:
  - go test .
  enforce: true
